package types

const(
	MSG_TYPE_REQ_LASTBLOCK = "reqLastBlock"
	MSG_TYPE_RESP_LASTBLOCK = "respLastBlock"

	MSG_TYPE_REQ_BLOCKBYNUMBER = "reqBlockByNumber"

	MSG_TYPE_BLOCK = "block"
)


const(
	WALLET_DIR = "./walletdir"
)

const(
	DIG_REWARD = 500
)